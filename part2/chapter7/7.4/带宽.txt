void dfs
 若已经填满
   更新最小带宽min为minimum(min, found)
 否则
   遍历每个点
     若点i未被用过
	若此点到已填序列内每个它的邻接点的距离的最大值小于min且其相邻节点中未被填进去的个数小于min
	  将点i填入cur
	  标记此点已用
	  更新整个序列的最大带宽found
	  试填下一个点
	  取消标记
不知道自己写的程序对不对，求喷~~